apiVersion: builtin
kind: SecretGenerator
metadata:
  name: kubernetes-dashboard-tls-secret
behavior: create
files:
  - tls.crt=certs/kubernetes-dashboard.crt
  - tls.key=certs/kubernetes-dashboard.key
type: kubernetes.io/tls
options:
  disableNameSuffixHash: true
